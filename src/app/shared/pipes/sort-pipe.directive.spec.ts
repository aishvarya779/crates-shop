import { SortPipeDirective } from './sort-pipe.directive';

describe('SortPipeDirective', () => {
  it('should create an instance', () => {
    const directive = new SortPipeDirective();
    expect(directive).toBeTruthy();
  });
});
